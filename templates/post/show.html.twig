{% extends 'base.html.twig' %}

{% block title %}Detail du post{% endblock %}

{% block body %}

    <div class="example-wrapper">
        <h1>{{ post.title }}</h1>

        <p>Créée le {{ post.getCreatedAt()|date('Y-m-d H:i:s')}} par {{ post.user.username }}</p>
        <img src="#" alt="">
        <p>{{ post.content }}</p>

    </div>
{% endblock %}
